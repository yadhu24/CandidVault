import { SectionPlaceholder } from '../SectionPlaceholder'

export default function EventAlbumsPage() {
  return (
    <SectionPlaceholder title="Albums" description="Organize media into curated albums here." />
  )
}
